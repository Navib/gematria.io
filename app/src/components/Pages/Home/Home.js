import React from 'react';

import Calculator from '../../../containers/Calculator';

const Home = ({ children }) => {
  return (
    <div>
      <Calculator />
    </div>
  );
};

export default Home;
